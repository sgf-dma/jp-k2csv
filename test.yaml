
- name: "test"
  files: &files
    dest: "./vforms/test"
  filter: &filter
    from: 14
    till: 30
  questions:
    - front:
        - line:
          - vform:
              filter:
                - "intransitive"
              base: "dictBased"
              new:  ""
      back:
        line: &potentialL
        - vform: &potential
            rowMod: "transPair"
            filter:
              - "v2"
            base: "teBased"
            new:  "た"
        - vform: &potential
            rowMod: "id"
            filter:
              - "v2"
            base: "teBased"
            new:  "て"

