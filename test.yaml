
- name: "test"
  files: &files
    dest: "./vforms/test"
  filter: &filter
    from: 0
  questions:
    - front:
        - line:
          - vform:
              filter:
                tags:
                  - "intransitive"
              base: "dictBased"
              new:  ""
      back:
        line:
        - vform:
            rowMod: "transPair"
            filter:
              tags:
                - "v2"
            base: "teBased"
            new:  "た"
        - vform:
            rowMod: "id"
            filter:
              tags:
                - "v2"
            base: "teBased"
            new:  "て"

